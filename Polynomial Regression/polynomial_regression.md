# Polynomial Regression

Similarly to linear regression, polynomial regression is a supervised learning algorithm.

It fits a sequence of data points onto a polynomial curve, the user specifies which degree polynomial to fit the points to. Used when points do not fit a line but can fit onto a polynomial curve