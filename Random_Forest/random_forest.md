# Random Forest

Random Forests are an ensemble learning technique, which uses a varying number of decision trees, each of which gets a random subset of data with a random subset of features to train on. Predictions are then done through passing the data through every tree, getting each tree's classification then performing a majority vote.