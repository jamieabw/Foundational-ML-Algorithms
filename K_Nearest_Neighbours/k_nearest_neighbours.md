# K Nearest Neighbours

KNN is a supervised learning technique which uses given data points and their classifications to predict the classifications of new data points. It does this by finding the K nearest neighbouring points in terms of Euclidean distance, then uses their classifications to perform a majority vote on what the classification of the new data point should be.